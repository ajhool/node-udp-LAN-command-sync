{
  "name": "synced-remote-command-client",
  "version": "1.0.0",
  "description": "A client that receives commands from a remote connection and executes them synchronously with the other clients",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js --exec babel-node --presets es2015,stage-2"
  },
  "keywords": [
    "sync",
    "remote",
    "command",
    "internet",
    "of",
    "things"
  ],
  "author": "Aidan Hoolachan",
  "license": "MIT",
  "dependencies": {
    "dgram": "^1.0.1",
    "ws": "^2.3.1"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-plugin-transform-flow-strip-types": "^6.22.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1",
    "nodemon": "^1.11.0"
  }
}
